package Runner;
import com.Encapsulation.Bind;

import com.Encapsulation.Bind;

public class BindRunner {
    public static void main(String[] args) {
        Bind bind=new Bind();

        System.out.println(bind.getBook());
    }
}
